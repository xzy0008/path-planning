function out = pathInsidePolygon(boundingPolygon, testPath)
%PATHINSIDEPOLYGON Determines if a path is inside a given polygon
%	Calls segmentInsidePolygon on each segment of testPath and returns
%	result
%	
%   Syntax:  pathInsidePolygon(boundingPolygon, testPath)
%       - obstaclePolygons =ppPolygon structure to check to see if path
%           lies within
%       - testPath - array of ppSegment structures containing path to check
%
%   Output: 
%       returns true if path is completely inside polygon, false otherwise
%

out=true;

% check each segment to see if it lies within polygon
for ii=1:length(testPath)
	if (~segmentInsidePolygon(boundingPolygon,testPath(ii)))
		out=false;
		return;
	end
end

end