function out = pathCollisionFree(obstaclePolygons, testPath)
%PATHCOLLISIONFREE Determines if a path is collision free
%	Calls segmentCollisionFree on each segment of testPath and returns
%	result
%	
%   Syntax:  pathCollisionFree(obstaclePolygons, testPath)
%       - obstaclePolygons = array of ppPolygon structures that define the
%         obstacles
%       - testPath - array of ppSegment structures containing path to check
%           for collisions
%
%   Output: 
%       returns true if no collisions occur on path, false otherwise
%

out=true;

for ii=1:length(testPath)
	if (~segmentCollisionFree(obstaclePolygons,testPath(ii)))
		out=false;
		return;
	end
end

end